---
import ContactButton from './ContactButton.astro';
import CheckIcon from '../icons/CheckIcon.svg';
interface Props {
  features: string[];
}

const { features } = Astro.props;
---

<section class="container mx-auto px-6 py-8">
  <h2 class="text-3xl font-bold text-center mb-8">Ofrecemos</h2>
  <ul class="grid grid-cols-1 lg:grid-cols-2 mb-8">
    {
    features.map( feature => (
      <li class="py-2 flex flex-row gap-2"> <CheckIcon /> <span>{ feature }</span></li>
    ))
    }
  </ul>
  <div class="flex justify-center">
    <ContactButton link="/contacto" text="Pide presupuesto" />
  </div>
</section>
